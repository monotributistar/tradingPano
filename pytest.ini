[pytest]
testpaths = tests
addopts =
    -v
    --tb=short
    -p no:warnings
    --strict-markers
markers =
    unit: pure unit tests (no I/O, no DB, no network)
    api:  FastAPI integration tests using TestClient + in-memory SQLite
    slow: tests that take > 5 s (excluded from CI fast run)
